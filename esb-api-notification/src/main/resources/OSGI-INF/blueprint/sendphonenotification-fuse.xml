<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
    xmlns:camel="http://camel.apache.org/schema/blueprint"
    xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
    xmlns:cxf="http://camel.apache.org/schema/blueprint/cxf"
    xmlns:jaxws="http://cxf.apache.org/blueprint/jaxws"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://cxf.apache.org/transports/camel http://cxf.apache.org/transports/camel.xsd        https://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd        http://camel.apache.org/schema/blueprint/cxf http://camel.apache.org/schema/blueprint/cxf/camel-cxf.xsd        http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">
    <routeContext id="notificationContext-fuse" xmlns="http://camel.apache.org/schema/blueprint">
        <route autoStartup="false" id="batchSchedule" routePolicyRef="batchSchedulePolicy">
            <from uri="activemqcompcop:{{COP_CLAIMNOTIFICATION_BATCH_QUEUE}}"/>
            <to uri="activemqcompcop:{{COP_CLAIMNOTIFICATION_REAL_QUEUE}}"/>
            <log message="lognotify: The messages in the batch queue are being moved to real-time queue"/>
        </route>
        <route autoStartup="false" id="retrySchedule" routePolicyRef="retrySchedulePolicy">
            <from uri="activemqcompcop:{{COP_CLAIMNOTIFICATION_RETRY_QUEUE}}"/>
            <to uri="activemqcompcop:{{COP_CLAIMNOTIFICATION_REAL_QUEUE}}"/>
            <log message="lognotify: The messages in the retry queue are being moved to real-time queue"/>
        </route>
        <route id="sendPhoneNotification">
            <from uri="direct:sendPhoneNotification"/>
            <log message="lognotify: send phone notification transaction starts"/>
            <doTry>
                <log message="lognotify: To logging the inbound request"/>
                <setProperty propertyName="transactionLog">
                    <simple>ref:transactionLog</simple>
                </setProperty>
                <setHeader headerName="logOperation">
                    <simple>insertInboundRequest</simple>
                </setHeader>
                <log message="lognotify: To prepare the logging MSG_IDENTIFIER"/>
                <process ref="phoneNotificationFuseRqLogging"/>
                <to uri="direct-vm:transactionloggingGateway"/>
                <removeProperty propertyName="APPLICATION_ID"/>
                <log message="lognotify: To validate APPLICATION_ID in soapheader and message schema"/>
                <process ref="phoneNotificationFuseRqValidator"/>
                <log message="lognotify: To map the request"/>
                <process ref="phoneNotificationFuseRqMapper"/>
                <doTry>
                    <log message="lognotify: To notification request mapper"/>
                    <setProperty propertyName="APPLICATION_ID">
                        <simple>CC</simple>
                    </setProperty>
                    <log message="lognotify: To invoke notification center"/>
                    <choice>
                        <when>
                            <simple>${header.COP_NOTIFICATION_QUEUE} == 'COP_CLAIMNOTIFICATION_REAL_QUEUE'</simple>
                            <to pattern="InOnly" uri="activemqcompcop:{{COP_CLAIMNOTIFICATION_REAL_QUEUE}}"/>
                        </when>
                        <when>
                            <simple>${header.COP_NOTIFICATION_QUEUE} == 'COP_CLAIMNOTIFICATION_BATCH_QUEUE'</simple>
                            <to pattern="InOnly" uri="activemqcompcop:{{COP_CLAIMNOTIFICATION_BATCH_QUEUE}}"/>
                        </when>
                    </choice>
                    <log message="lognotify: message has been placed into ${header.COP_NOTIFICATION_QUEUE} queue"/>
                </doTry>
                <log message="lognotify: To create notification response mapping"/>
                <process ref="phoneNotificationFuseRsMapper"/>
                <log message="lognotify: To log the outbound response message"/>
                <setHeader headerName="logOperation">
                    <simple>updateInboundResponse</simple>
                </setHeader>
                <to uri="direct-vm:transactionloggingGateway"/>
                <doCatch>
                    <exception>java.lang.Exception</exception>
                    <handled>
                        <constant>true</constant>
                    </handled>
                    <to uri="direct:sendPhoneNotificationFuseErrorResponse"/>
                </doCatch>
            </doTry>
            <bean method="setResponse" ref="genericTransactionLogging"/>
            <log message="lognotify: create notification transaction is completed"/>
        </route>
        <route id="direct:sendPhoneNotificationFuseResponseErrorRoute">
            <from uri="direct:sendPhoneNotificationFuseErrorResponse"/>
            <doTry>
                <log message="lognotify: To generate the response with error mapper"/>
                <process ref="phoneNotificationFuseRsErrorMapper"/>
                <log message="lognotify: To log the response error"/>
                <setHeader headerName="logOperation">
                    <simple>updateError</simple>
                </setHeader>
                <to uri="direct-vm:transactionloggingGateway"/>
                <log message="lognotify: End of logging the response error"/>
                <doCatch>
                    <exception>java.lang.Exception</exception>
                    <handled>
                        <constant>false</constant>
                    </handled>
                    <setHeader headerName="logOperation">
                        <simple>updateError</simple>
                    </setHeader>
                    <to uri="direct-vm:transactionloggingGateway"/>
                </doCatch>
            </doTry>
        </route>
    </routeContext>
</blueprint>
