<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
	xmlns:cm="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0"
	xmlns:cxf="http://camel.apache.org/schema/blueprint/cxf" xmlns:enc="http://karaf.apache.org/xmlns/jasypt/v1.0.0"
	xmlns:ext="http://aries.apache.org/blueprint/xmlns/blueprint-ext/v1.0.0"
	xmlns:http="http://cxf.apache.org/transports/http/configuration"
	xmlns:sec="http://cxf.apache.org/configuration/security" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.osgi.org/xmlns/blueprint/v1.0.0 https://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd                            http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">
	<routeContext id="logging_routeContext"
		xmlns="http://camel.apache.org/schema/blueprint">
		<route id="logOutboundDeatils">
			<from uri="direct:logOutboundDeatils" />
			<doTry>
				<bean method="setOutboundDetails" ref="loggingProcessor" />
				<to pattern="InOnly" uri="vm:asynctransactionlog" />
				<doCatch>
					<exception>java.lang.Exception</exception>
					<handled>
						<constant>true</constant>
					</handled>
				</doCatch>
			</doTry>
		</route>
		<route id="logOuboundError">
			<from uri="direct:logOuboundError" />
			<bean method="setErrorDetails" ref="loggingProcessor" />
			<to uri="direct:logOutboundDeatils" />
		</route>
		<route id="logOutboundSuccess">
			<from uri="direct:logOutboundSuccess" />
			<setProperty propertyName="STATUS">
				<simple resultType="java.lang.String">SUCCESS</simple>
			</setProperty>
			<to uri="direct:logOutboundDeatils" />
		</route>
		<route>
			<from uri="direct:logInboundError" />
			<bean method="setErrorDetails" ref="loggingProcessor" />
			<to uri="direct:logInboundDetails" />
		</route>
		<route id="logInboundSuccess">
			<from uri="direct:logInboundSuccess" />
			<bean method="setSuccessDetails" ref="loggingProcessor" />
			<to uri="direct:logInboundDetails" />
		</route>
		<route id="logInboundDetails">
			<from uri="direct:logInboundDetails" />
			<doTry>
				<bean method="setInboundDetails" ref="loggingProcessor" />
				<to pattern="InOnly" uri="vm:asynctransactionlog" />
				<doCatch>
					<exception>java.lang.Exception</exception>
					<handled>
						<constant>true</constant>
					</handled>
				</doCatch>
			</doTry>
			<setProperty propertyName="CamelRouteStop">
				<simple resultType="java.lang.Boolean">true</simple>
			</setProperty>
		</route>
	</routeContext>
</blueprint>
